# Load the ALSA loopback at boot for the native RetroArch addon's audio capture:
# RetroArch plays to hw:Loopback,0,0 and the bridge's arecord reads the mirrored
# hw:Loopback,1,0. Installed to /etc/modules-load.d/mytesla-retroarch.conf by
# scripts/install-retroarch-addon.sh.
snd-aloop
